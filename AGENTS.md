# AGENTS.md

The full project guide lives in **`CLAUDE.md`** — read it first.

Critical, in case you only read this file: this is an **Expo SDK 54** project (deliberately
pinned to match the user's Expo Go — do **not** upgrade the SDK without asking). It's **Mosaik**,
a calm, premium, German-language impact/giving app branded to **LBBW** (navy `#123250` + signal
green `#37C391`, Hanken Grotesk, design tokens in `src/constants/theme.ts`). Keep the design
language intact and read the versioned Expo docs (v54) before writing Expo code.
